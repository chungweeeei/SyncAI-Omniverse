[project]
name = "syncai-omniverse"
version = "0.1.0"
description = "Warehouse simulation with Jetbot AMR on NVIDIA Isaac Sim 5.1"
requires-python = ">=3.10"

# Note: isaacsim, pxr, omni, carb are provided by the Isaac Sim runtime.
# Do NOT pip-install them separately.
dependencies = [
    "pyyaml",
]
